INSERT INTO {TARGET_TABLE_NAME}(
            uuid,
            eligibility_uuid,
            fingerprint,
            email,
            address,
            phone_number,
            first_name,
            last_name,
            state,
            preferred_language,
            country,
            is_eligible,
            gender,
            client_name,
            client_id,
            city,
            date_of_birth,
            program_type,
            enrollment_status,
            status,
            onboarding_call_date,
            onboarding_call_status,
            activation_date,
            completion_date,
            total_sessions,
            last_session_date,
            treatment_assigned_pt,
            kit_type,
            sword_id,
            excluded_reason,
            sword_uuid,
            account_uuid,
            onboarding_call_reschedule_link,
            relationship_type,
            enrollment_started_date,
            enrollment_completed_date,
            first_recruitable_date,
            client_logo,
            zip_code,
            current_step,
            last_app_login,
            eligible_for,
            employer_business_unit,
            group_policy_id,
            high_risk
            )
        SELECT DISTINCT
            aux.uuid,
            aux.eligibility_uuid,
            aux.fingerprint,
            aux.email,
            aux.address,
            aux.phone_number,
            aux.first_name,
            aux.last_name,
            aux.state,
            aux.preferred_language,
            aux.country,
            aux.is_eligible,
            aux.gender,
            aux.client_name,
            aux.client_id,
            aux.city,
            aux.date_of_birth,
            aux.program_type,
            aux.enrollment_status,
            aux.status,
            aux.onboarding_call_date,
            aux.onboarding_call_status,
            aux.activation_date,
            aux.completion_date,
            aux.total_sessions,
            aux.last_session_date,
            aux.treatment_assigned_pt,
            aux.kit_type,
            aux.sword_id,
            aux.excluded_reason,
            aux.sword_uuid,
            aux.account_uuid,
            aux.onboarding_call_reschedule_link,
            aux.relationship_type,
            aux.enrollment_started_date,
            aux.enrollment_completed_date,
            aux.first_recruitable_date,
            aux.client_logo,
            aux.zip_code,
            aux.current_step,
            aux.last_app_login,
            aux.eligible_for,
            aux.employer_business_unit,
            aux.group_policy_id,
            aux.high_risk
        FROM {TARGET_TABLE_NAME_AUX} as aux
        WHERE
        aux.sword_uuid IS NULL AND
        aux.uuid NOT IN (SELECT uuid FROM lead) AND
        aux.fingerprint NOT IN (SELECT DISTINCT fingerprint FROM lead WHERE fingerprint IS NOT NULL)